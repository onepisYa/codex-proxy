## codex-proxy docs

This directory documents proxy-specific behavior that is not part of the upstream OpenAI API surface.

### Topics

- `sessions.md`: Provider-independent session keys and sticky routing.
- `auto-compaction.md`: Auto-compaction retry behavior and compatibility notes.
- `models.md`: Periodic model discovery and `GET /v1/models` behavior.

